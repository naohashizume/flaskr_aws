[Unit]
Description=Flaskr service
After=network.target

[Service]
WorkingDirectory=/home/centos/flaskr
Type=simple
User=centos
Environment=FLASK_APP=flaskr
ExecStart=/bin/bash -c 'flask run --host=0.0.0.0'
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
